# Интеграция синхронизации Firebase

## 📋 Шаги интеграции

### Шаг 1: Добавьте Firebase SDK в index.html

Найдите секцию со скриптами в `index.html` и добавьте **ПЕРЕД** вашими модулями:

```html
<!-- Firebase SDK -->
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>
```

Должно получиться так:

```html
<!-- Скрипты -->
<!-- Firebase SDK -->
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.7.1/firebase-firestore-compat.js"></script>

<!-- Flatpickr -->
<script src="https://cdn.jsdelivr.net/npm/flatpickr" crossorigin="anonymous"></script>
<!-- ... остальные скрипты -->
```

### Шаг 2: Настройте firebase-config.js

1. Откройте `js/firebase-config.js`
2. Замените значения на свои из Firebase Console
3. Сохраните файл

### Шаг 3: Обновите auth.js для работы с Firebase

Вместо локальной авторизации используйте Firebase:

```javascript
// В начале файла auth.js добавьте:
import { signUp as firebaseSignUp, signIn as firebaseSignIn, signOut as firebaseSignOut } from './sync-firebase.js';

// Обновите функцию createUser:
export async function createUser(name, email, password) {
    // Используем Firebase для регистрации
    const result = await firebaseSignUp(email, password);
    if (result.success) {
        // Сохраняем имя пользователя в Firestore
        // ... код сохранения имени
        return { success: true, user: result.user };
    }
    return result;
}

// Аналогично для loginUser
```

### Шаг 4: Добавьте автосинхронизацию

В `js/app.js` или в месте где обрабатываются изменения задач:

```javascript
import { syncTasks, loadTasks, subscribeToTasks } from './sync-firebase.js';
import { onAuthStateChanged } from './sync-firebase.js';

// При загрузке приложения
onAuthStateChanged(async (user) => {
    if (user) {
        // Пользователь авторизован - загружаем данные из Firebase
        const result = await loadTasks();
        if (result.success) {
            // Обновляем локальное состояние
            stateManager.set('tasks', result.tasks);
        }
        
        // Подписываемся на изменения в реальном времени
        subscribeToTasks((tasks) => {
            stateManager.set('tasks', tasks);
            StorageManager.set('flowTasks', tasks, user.uid);
        });
    }
});

// При изменении задач - синхронизируем
stateManager.subscribe('tasks', async (newTasks) => {
    const user = getCurrentUser();
    if (user) {
        await syncTasks(newTasks);
    }
});
```

### Шаг 5: Добавьте кнопку синхронизации (опционально)

В настройках можно добавить кнопку "Синхронизировать сейчас":

```javascript
import { syncAll, loadAll } from './sync-firebase.js';

// В обработчике кнопки синхронизации
async function handleSync() {
    const result = await syncAll();
    if (result.success) {
        showNotification('Данные синхронизированы');
    } else {
        showNotification('Ошибка синхронизации: ' + result.error);
    }
}
```

## 🔄 Как это работает

1. **При входе**: Данные загружаются из Firebase
2. **При создании задачи**: Сохраняется локально + отправляется в Firebase
3. **На другом устройстве**: Данные автоматически обновляются через подписку
4. **Офлайн**: Работает локально, синхронизируется при подключении

## 🧪 Тестирование

1. Создайте аккаунт на одном устройстве
2. Добавьте несколько задач
3. Войдите на другом устройстве
4. Задачи должны появиться автоматически

## ⚠️ Важные замечания

- **Офлайн режим**: Firebase автоматически кэширует данные для офлайн работы
- **Конфликты**: При одновременном редактировании на разных устройствах последнее изменение побеждает
- **Безопасность**: Правила Firestore защищают данные пользователей

## 🐛 Решение проблем

### Данные не синхронизируются
- Проверьте консоль браузера на ошибки
- Убедитесь что Firebase инициализирован
- Проверьте правила Firestore

### "Permission denied"
- Проверьте что пользователь авторизован
- Проверьте правила Firestore (должны быть как в FIREBASE_SETUP.md)

---

Готово! Теперь ваше приложение синхронизируется между устройствами. 🎉


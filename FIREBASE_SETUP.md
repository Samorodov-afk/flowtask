# Настройка Firebase для синхронизации

## 🚀 Быстрая настройка (5 минут)

### Шаг 1: Создайте проект Firebase

1. Перейдите на https://console.firebase.google.com/
2. Нажмите "Добавить проект"
3. Введите название проекта (например, "FlowTask")
4. Отключите Google Analytics (не обязательно)
5. Нажмите "Создать проект"

### Шаг 2: Включите Authentication

1. В меню слева выберите **Authentication**
2. Нажмите **Get started**
3. Включите **Email/Password** провайдер:
   - Нажмите на "Email/Password"
   - Включите "Email/Password"
   - Сохраните

### Шаг 3: Создайте Firestore Database

1. В меню слева выберите **Firestore Database**
2. Нажмите **Create database**
3. Выберите **Start in test mode** (для начала)
4. Выберите регион (ближайший к вам)
5. Нажмите **Enable**

### Шаг 4: Получите конфигурацию

1. Нажмите на иконку ⚙️ (Settings) → **Project settings**
2. Прокрутите вниз до "Your apps"
3. Нажмите на иконку **Web** (`</>`)
4. Зарегистрируйте приложение (название: "FlowTask Web")
5. Скопируйте конфигурацию (она будет выглядеть так):

```javascript
const firebaseConfig = {
  apiKey: "AIza...",
  authDomain: "your-project.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project.appspot.com",
  messagingSenderId: "123456789",
  appId: "1:123456789:web:abc123"
};
```

### Шаг 5: Настройте правила безопасности

1. Перейдите в **Firestore Database** → **Rules**
2. Замените правила на:

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Пользователи могут читать/писать только свои данные
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
      
      // Задачи пользователя
      match /tasks/{taskId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Категории пользователя
      match /categories/{categoryId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
      
      // Быстрые задачи пользователя
      match /quickTasks/{quickTaskId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
```

3. Нажмите **Publish**

### Шаг 6: Добавьте Firebase в проект

1. Создайте файл `js/firebase-config.js` с вашей конфигурацией
2. Добавьте скрипты Firebase в `index.html`
3. Используйте модуль `js/sync-firebase.js` для синхронизации

---

## 📁 Структура данных в Firestore

```
users/
  {userId}/
    tasks/
      {taskId}/
        - text: string
        - priority: string
        - category: string
        - deadline: timestamp
        - completed: boolean
        - createdAt: timestamp
        - ...
    categories/
      {categoryId}/
        - name: string
        - color: string
        - ...
    quickTasks/
      {quickTaskId}/
        - text: string
        - priority: string
        - ...
    settings/
      - theme: string
      - language: string
      - notifications: object
```

---

## 🔒 Безопасность

⚠️ **Важно**: После настройки обновите правила Firestore для продакшена:

1. Перейдите в **Firestore Database** → **Rules**
2. Используйте правила из шага 5 выше
3. Для дополнительной безопасности можно добавить проверки полей

---

## 💰 Лимиты бесплатного плана

- **Firestore**: 1GB хранения, 50K чтений/день, 20K записей/день
- **Authentication**: Безлимитно
- **Hosting**: 10GB хранения, 360MB/день трафика

Для личного использования этого более чем достаточно!

---

## 🐛 Решение проблем

### Проблема: "Permission denied"
**Решение**: Проверьте правила Firestore - убедитесь что пользователь авторизован

### Проблема: Данные не синхронизируются
**Решение**: Проверьте консоль браузера на ошибки, убедитесь что Firebase инициализирован

### Проблема: "Quota exceeded"
**Решение**: Вы превысили лимит бесплатного плана, нужно перейти на платный

---

Готово! Теперь можно использовать синхронизацию. 🎉


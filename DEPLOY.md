# Инструкция по деплою на GitLab Pages

## 🚀 Быстрый старт

1. **Создайте репозиторий на GitLab**
   - Войдите в GitLab
   - Создайте новый проект
   - Скопируйте URL репозитория

2. **Инициализируйте Git локально** (если еще не сделано)
   ```bash
   cd c:\Users\user\Desktop\Work\Work\Coursor
   git init
   git add .
   git commit -m "Initial commit: FlowTask оптимизирован для GitLab"
   ```

3. **Добавьте удаленный репозиторий**
   ```bash
   git remote add origin <URL_вашего_репозитория>
   git branch -M main
   git push -u origin main
   ```

4. **Настройте GitLab Pages**
   - Перейдите в Settings → Pages
   - GitLab автоматически обнаружит `.gitlab-ci.yml`
   - После первого пуша в ветку `main` начнется деплой
   - Через несколько минут сайт будет доступен по адресу:
     `https://<username>.gitlab.io/<project-name>/`

## ⚙️ Настройка для подпапок

Если ваш проект находится в подпапке на GitLab Pages (например, `username.gitlab.io/project-name/`), обновите:

### 1. manifest.json
```json
{
  "start_url": "/project-name/",
  "scope": "/project-name/"
}
```

### 2. index.html (регистрация Service Worker)
Найдите код регистрации Service Worker и обновите путь:
```javascript
if ('serviceWorker' in navigator) {
  navigator.serviceWorker.register('/project-name/sw.js')
    .then(registration => console.log('SW registered'))
    .catch(error => console.log('SW registration failed'));
}
```

## 🔧 Проверка после деплоя

1. **Откройте сайт** по адресу GitLab Pages
2. **Проверьте консоль браузера** (F12) на наличие ошибок
3. **Проверьте Service Worker**:
   - Откройте DevTools → Application → Service Workers
   - Убедитесь что SW зарегистрирован
4. **Проверьте PWA**:
   - Откройте DevTools → Application → Manifest
   - Проверьте что манифест загружается
5. **Проверьте офлайн режим**:
   - Откройте DevTools → Network → Offline
   - Обновите страницу
   - Должна открыться `offline.html`

## 📝 Важные замечания

### HTTPS обязателен
- Service Worker работает только на HTTPS
- GitLab Pages автоматически предоставляет HTTPS
- Локально можно использовать `http://localhost` (исключение для разработки)

### Кэширование
- Service Worker кэширует ресурсы для офлайн работы
- При обновлении кода измените `CACHE_NAME` в `sw.js` для обновления кэша
- Пользователям может потребоваться обновить страницу несколько раз

### Внешние ресурсы (CDN)
- Font Awesome, Google Fonts, Flatpickr загружаются с CDN
- Они не кэшируются Service Worker (намеренно)
- Для полного офлайн режима можно скачать и разместить локально

## 🐛 Решение проблем

### Проблема: Service Worker не регистрируется
**Решение**: Проверьте что сайт открыт по HTTPS, а не HTTP

### Проблема: Ресурсы не загружаются (404)
**Решение**: Проверьте пути в `sw.js` - они должны быть относительными (`./`)

### Проблема: Стили не применяются
**Решение**: Проверьте что `style.css` находится в корне проекта

### Проблема: Модули не загружаются
**Решение**: Убедитесь что все файлы в папке `js/` присутствуют и имеют правильные пути

## 📦 Структура для GitLab Pages

```
public/                    # Автоматически создается GitLab CI
├── index.html
├── offline.html
├── style.css
├── script.js
├── sw.js
├── manifest.json
└── js/
    ├── app.js
    ├── auth.js
    └── ...
```

## 🔄 Обновление сайта

После каждого изменения:
```bash
git add .
git commit -m "Описание изменений"
git push origin main
```

GitLab автоматически запустит CI/CD и задеплоит изменения.

## 📊 Мониторинг

- **CI/CD Pipeline**: Settings → CI/CD → Pipelines
- **Pages**: Settings → Pages
- **Логи**: CI/CD → Pipelines → выберите pipeline → View logs

---

**Готово!** Ваш сайт должен работать на GitLab Pages. 🎉


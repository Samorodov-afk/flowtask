# Варианты синхронизации данных между устройствами

## 🎯 Цель
Сделать так, чтобы пользователь мог зайти на любом устройстве в свой аккаунт и видеть все свои задачи.

## 📊 Варианты решения

### Вариант 1: Supabase (РЕКОМЕНДУЕТСЯ) ⭐
**Плюсы:**
- ✅ Бесплатный тариф (до 500MB базы данных)
- ✅ Готовая аутентификация
- ✅ Real-time синхронизация
- ✅ Простая настройка
- ✅ Не нужен собственный сервер

**Минусы:**
- ⚠️ Зависимость от внешнего сервиса

**Стоимость:** Бесплатно до 500MB БД

---

### Вариант 2: Firebase (Google)
**Плюсы:**
- ✅ Бесплатный тариф (щедрый)
- ✅ Real-time синхронизация
- ✅ Готовая аутентификация
- ✅ Уже есть файл `sync-firebase.js`

**Минусы:**
- ⚠️ Зависимость от Google
- ⚠️ Может быть сложнее для новичков

**Стоимость:** Бесплатно до 1GB хранилища

---

### Вариант 3: Собственный Backend (Node.js/PHP)
**Плюсы:**
- ✅ Полный контроль
- ✅ Нет зависимости от внешних сервисов
- ✅ Можно хостить на GitLab (требует отдельный сервер)

**Минусы:**
- ❌ Нужен сервер ($$$)
- ❌ Нужно поддерживать код
- ❌ Сложнее настройка

**Стоимость:** От $5-10/месяц за VPS

---

### Вариант 4: GitLab + Backend API
**Плюсы:**
- ✅ Можно использовать GitLab CI/CD
- ✅ Интеграция с GitLab

**Минусы:**
- ❌ GitLab Pages не поддерживает backend
- ❌ Нужен отдельный сервер для API

**Стоимость:** От $5-10/месяц

---

## 🚀 Рекомендация: Supabase

**Почему Supabase:**
1. Проще всего настроить
2. Бесплатный тариф достаточен для начала
3. Отличная документация
4. PostgreSQL база данных (надежная)
5. Автоматическая генерация API

## 📝 Что нужно сделать

### Шаг 1: Создать аккаунт на Supabase
1. Зайдите на https://supabase.com
2. Создайте бесплатный аккаунт
3. Создайте новый проект

### Шаг 2: Настроить базу данных
Создайте таблицы в Supabase SQL Editor:

```sql
-- Таблица пользователей
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  email TEXT UNIQUE NOT NULL,
  name TEXT NOT NULL,
  password_hash TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица задач
CREATE TABLE tasks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  text TEXT NOT NULL,
  priority TEXT NOT NULL,
  category TEXT NOT NULL,
  deadline TIMESTAMP,
  completed BOOLEAN DEFAULT FALSE,
  completed_at TIMESTAMP,
  archived BOOLEAN DEFAULT FALSE,
  notes TEXT,
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);

-- Таблица категорий
CREATE TABLE categories (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  color TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Таблица быстрых задач
CREATE TABLE quick_tasks (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID REFERENCES users(id) ON DELETE CASCADE,
  text TEXT NOT NULL,
  priority TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- Индексы для быстрого поиска
CREATE INDEX idx_tasks_user_id ON tasks(user_id);
CREATE INDEX idx_categories_user_id ON categories(user_id);
CREATE INDEX idx_quick_tasks_user_id ON quick_tasks(user_id);
```

### Шаг 3: Настроить Row Level Security (RLS)
Включите RLS и создайте политики:

```sql
-- Включить RLS
ALTER TABLE users ENABLE ROW LEVEL SECURITY;
ALTER TABLE tasks ENABLE ROW LEVEL SECURITY;
ALTER TABLE categories ENABLE ROW LEVEL SECURITY;
ALTER TABLE quick_tasks ENABLE ROW LEVEL SECURITY;

-- Политики для users (пользователи видят только себя)
CREATE POLICY "Users can view own data" ON users
  FOR SELECT USING (auth.uid() = id);

-- Политики для tasks
CREATE POLICY "Users can manage own tasks" ON tasks
  FOR ALL USING (auth.uid() = user_id);

-- Политики для categories
CREATE POLICY "Users can manage own categories" ON categories
  FOR ALL USING (auth.uid() = user_id);

-- Политики для quick_tasks
CREATE POLICY "Users can manage own quick tasks" ON quick_tasks
  FOR ALL USING (auth.uid() = user_id);
```

### Шаг 4: Получить ключи API
В настройках проекта Supabase:
- Project URL
- Anon (public) key
- Service role key (секретный, не используйте в клиенте!)

### Шаг 5: Установить Supabase клиент
```bash
# В папке проекта (если используете npm)
npm install @supabase/supabase-js
```

Или подключить через CDN в `index.html`:
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
```

---

## 🔄 Миграция с localStorage на Supabase

Нужно будет:
1. Создать модуль `js/sync-supabase.js`
2. Обновить `js/storage.js` для работы с Supabase
3. Обновить `js/auth.js` для использования Supabase Auth
4. Добавить синхронизацию при загрузке и изменении данных

---

## 💡 Альтернатива: Гибридный подход

Можно сделать так:
- **Офлайн**: данные сохраняются в localStorage
- **Онлайн**: данные синхронизируются с Supabase
- **Автосинхронизация**: при подключении к интернету

Это даст:
- ✅ Работу без интернета
- ✅ Синхронизацию между устройствами
- ✅ Резервное копирование

---

## 📊 Сравнение вариантов

| Критерий | Supabase | Firebase | Свой Backend |
|----------|----------|----------|--------------|
| Сложность | ⭐ Легко | ⭐⭐ Средне | ⭐⭐⭐ Сложно |
| Стоимость | Бесплатно | Бесплатно | $5-10/мес |
| Настройка | 30 мин | 1 час | 1-2 дня |
| Поддержка | Нужна | Нужна | Полная |

---

## 🎯 Следующие шаги

1. Выберите вариант (рекомендую Supabase)
2. Создайте аккаунт и проект
3. Настройте базу данных
4. Я помогу создать код для синхронизации

---

**Готов помочь с реализацией любого варианта!** 🚀

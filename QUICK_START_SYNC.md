# 🚀 Быстрый старт: Синхронизация между устройствами

## За 5 минут до синхронизации!

### Шаг 1: Выберите сервис (рекомендую Supabase)

**Supabase** - самый простой вариант:
- ✅ Бесплатно
- ✅ 5 минут настройки
- ✅ Готовая база данных

### Шаг 2: Создайте проект на Supabase

1. Зайдите на https://supabase.com
2. Создайте аккаунт (через GitHub)
3. Создайте новый проект
4. Подождите 2-3 минуты

### Шаг 3: Получите ключи

1. В Supabase Dashboard → **Settings** → **API**
2. Скопируйте:
   - **Project URL**
   - **anon public** key

### Шаг 4: Создайте таблицы

1. В Supabase Dashboard → **SQL Editor**
2. Нажмите **New query**
3. Скопируйте SQL из файла `SUPABASE_SETUP.md` (Шаг 3)
4. Нажмите **Run**

### Шаг 5: Подключите к приложению

1. Скопируйте `js/config.example.js` в `js/config.js`
2. Вставьте ваши ключи из Supabase
3. Установите `enabled: true`

```javascript
export const SUPABASE_CONFIG = {
    url: 'https://ваш-проект.supabase.co',
    anonKey: 'ваш-ключ-здесь',
    enabled: true
};
```

### Шаг 6: Добавьте Supabase в HTML

Откройте `index.html` и добавьте перед `</body>`:

```html
<!-- Supabase Client -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>
```

### Шаг 7: Обновите app.js

В `js/app.js` добавьте:

```javascript
import { initSupabase } from './sync-supabase.js';
import { SUPABASE_CONFIG } from './config.js';

// Инициализация Supabase
if (SUPABASE_CONFIG.enabled && SUPABASE_CONFIG.url && SUPABASE_CONFIG.anonKey) {
    initSupabase(SUPABASE_CONFIG.url, SUPABASE_CONFIG.anonKey);
    console.log('✅ Синхронизация включена');
}
```

### Шаг 8: Обновите auth.js

Замените функции регистрации/входа на Supabase версии (см. `sync-supabase.js`)

### Готово! 🎉

Теперь:
- Зарегистрируйтесь в приложении
- Создайте задачу
- Откройте Supabase Dashboard → Table Editor → tasks
- Увидите вашу задачу в базе!

---

## 📱 Проверка синхронизации

1. Откройте приложение на телефоне
2. Войдите в тот же аккаунт
3. Задачи должны появиться автоматически!

---

## ❓ Проблемы?

Смотрите `SUPABASE_SETUP.md` для детальной инструкции и решения проблем.

---

**Время настройки: ~10 минут** ⏱️

